name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  APP_DIR: /opt/tower-finder-service

# Every job reads the repository and nothing more; a job that needs another
# scope declares its own.
permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
      # setup-python owns the interpreter and uv syncs .venv from the lock.
      # --locked fails the job when pyproject.toml has moved without uv.lock.
      # The venv goes on PATH so the steps below find ruff and pytest there.
      # uv at the Dockerfile's UV_VERSION, so a lock the image cannot sync fails
      # here rather than at the staging build. An unreadable version fails the
      # job, since an empty one would leave setup-uv on its latest.
      - id: uv
        run: |
          version="$(sed -n 's/^ARG UV_VERSION=//p' Dockerfile)"
          [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]] || {
            echo "Expected one UV_VERSION in the Dockerfile, found: '$version'"; exit 1; }
          echo "version=$version" >> "$GITHUB_OUTPUT"
      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: ${{ steps.uv.outputs.version }}
      - run: uv sync --locked && echo "$PWD/.venv/bin" >> "$GITHUB_PATH"
      - name: Lint (ruff)
        run: ruff check .
      - name: Format check (ruff)
        run: ruff format --check .
      - name: Unit tests
        run: pytest -m "not integration" -q

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Unit tests
        run: npm test
      - name: Build
        run: npm run build
      - name: E2E
        # Serves the built dist with `vite preview`; the specs mock /api
        # themselves, so this needs no backend and no FCC credentials.
        run: |
          npx playwright install --with-deps chromium
          npm run test:e2e

  deploy-staging:
    runs-on: ubuntu-latest
    needs: [test, frontend]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    concurrency:
      group: tf-svc-deploy-staging
      cancel-in-progress: false
    env:
      DEPLOY_REF: ${{ github.sha }}
    steps:
      - name: Check required secrets
        env:
          STAGING_HOST: ${{ secrets.STAGING_HOST }}
          STAGING_SSH_KEY: ${{ secrets.STAGING_SSH_KEY }}
        run: |
          test -n "$STAGING_HOST" || { echo "STAGING_HOST is not set"; exit 1; }
          test -n "$STAGING_SSH_KEY" || { echo "STAGING_SSH_KEY is not set"; exit 1; }
      - name: Deploy to staging via SSH
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1.2.5
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: root
          key: ${{ secrets.STAGING_SSH_KEY }}
          command_timeout: 20m
          envs: APP_DIR,DEPLOY_REF
          script: |
            set -euo pipefail
            # Three near-identical droplets share this workflow. A mis-set
            # secret would otherwise deploy the wrong one, silently.
            test "$(hostname)" = "retina-staging" || {
              echo "Refusing to deploy staging to $(hostname)"; exit 1; }
            cd "$APP_DIR"
            # Deploy the commit this run is for, not whatever origin/main points
            # at now, so a second merge landing mid-run cannot slip an untested
            # commit past staging's smoke test. Hard-reset rather than `git pull`:
            # pull aborts silently on a divergent branch (a local commit on the
            # server), the build then caches every layer because the source
            # "didn't change", and the new code never ships.
            git fetch origin "$DEPLOY_REF"
            git reset --hard FETCH_HEAD
            test -f backend/.env || {
              echo "backend/.env is missing; secrets would be lost"; exit 1; }
            cp deploy/env.staging.example .env
            # Top up with this host's own CARTO basemap key — see the note in
            # deploy/env.<env>.example. Guarded rather than chained with &&: a host
            # with no key must build watermarked tiles, not fail its deploy.
            if [ -f /root/.secrets/carto.env ]; then cat /root/.secrets/carto.env >> .env; fi
            # Syntax-check the new config before anything replaces the running edge:
            # a template nginx rejects would otherwise leave a crash-looping
            # container where a working one was. Uses the droplet's real
            # certificates, which `nginx -t` insists on reading.
            docker compose build edge
            docker compose run --rm --no-deps -T edge nginx -t
            docker compose up -d --build
            healthy=""
            for i in $(seq 1 12); do
              if docker compose exec -T tower-finder-service \
                python3 -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')" 2>/dev/null; then
                echo "Deploy healthy after ~$((i * 5))s"
                healthy=1
                break
              fi
              echo "Waiting for health check... attempt $i/12"
              sleep 5
            done
            if [ -z "$healthy" ]; then
              echo "Health check failed after 60s"
              docker compose logs --tail=50 || true
              exit 1
            fi
            # The edge ships dark: nothing points at 8443 yet — the fleet still
            # arrives over retina-server's nginx on 443 — so the only way to
            # verify this listener before the Cloudflare Origin Rule flips is to
            # address it directly on the droplet, under the hostname it serves.
            # --resolve rather than DNS: every name resolves to Cloudflare, not to
            # the droplet, and this probe must reach the local listener directly.
            # An unverified peer needs a Cloudflare client certificate; this
            # probe is exempt because it arrives from the droplet itself.
            edge_ok=""
            for i in $(seq 1 12); do
              code="$(curl -sk -o /dev/null -w '%{http_code}' --max-time 10 \
                --resolve "staging-towers.retina.fm:8443:127.0.0.1" \
                "https://staging-towers.retina.fm:8443/api/health" || true)"
              if [ "$code" = "200" ]; then
                echo "Edge answered 200 on 8443 after ~$((i * 5))s"
                edge_ok=1
                break
              fi
              echo "Waiting for the edge on 8443... attempt $i/12 (last: ${code:-no answer})"
              sleep 5
            done
            if [ -z "$edge_ok" ]; then
              echo "Edge did not answer 200 on https://staging-towers.retina.fm:8443/api/health"
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            # The whole rendered config, not a sampled header: a header present
            # since the day it was added proves nothing about which config is
            # loaded. sed reproduces the entrypoint's envsubst exactly, because
            # TFS_EDGE_HOST is the template's only variable.
            sed 's|${TFS_EDGE_HOST}|staging-towers.retina.fm|g' deploy/nginx/edge.conf.template > /tmp/edge-expected.conf
            if ! docker compose exec -T edge cat /etc/nginx/conf.d/default.conf > /tmp/edge-running.conf; then
              echo "Could not read the edge's running config."
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            if ! diff -u /tmp/edge-expected.conf /tmp/edge-running.conf; then
              echo "The edge is serving a config this deploy did not render."
              exit 1
            fi
            # The document, not only the API: a frontend build that leaves no
            # index.html keeps /api/health at 200 while / becomes a 404.
            edge_doc="$(curl -sk -o /dev/null -w '%{http_code}' --max-time 30 \
              --resolve "staging-towers.retina.fm:8443:127.0.0.1" "https://staging-towers.retina.fm:8443/" || echo unreachable)"
            if [ "$edge_doc" != "200" ]; then
              echo "The edge served / as $edge_doc."
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            # Verification runs against the container directly rather than over
            # staging's public hostname: the droplet-local path cannot be blurred
            # by Cloudflare caching and proves this deploy, not whatever the
            # edge in front of the name still routes to.
            EXPECT_ENV=staging bash deploy/smoke-local.sh

  deploy-prod:
    runs-on: ubuntu-latest
    needs: [test, frontend, deploy-staging]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    concurrency:
      group: tf-svc-deploy-prod
      cancel-in-progress: false
    env:
      DEPLOY_REF: ${{ github.sha }}
    steps:
      - name: Check required secrets
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
        run: |
          test -n "$DEPLOY_HOST" || { echo "DEPLOY_HOST is not set"; exit 1; }
          test -n "$DEPLOY_SSH_KEY" || { echo "DEPLOY_SSH_KEY is not set"; exit 1; }
      - name: Deploy to production via SSH
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1.2.5
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          command_timeout: 20m
          envs: APP_DIR,DEPLOY_REF
          script: |
            set -euo pipefail
            test "$(hostname)" = "retina-prod" || {
              echo "Refusing to deploy production to $(hostname)"; exit 1; }
            cd "$APP_DIR"
            git fetch origin "$DEPLOY_REF"
            git reset --hard FETCH_HEAD
            test -f backend/.env || {
              echo "backend/.env is missing; secrets would be lost"; exit 1; }
            cp deploy/env.prod.example .env
            # Top up with this host's own CARTO basemap key — see the note in
            # deploy/env.<env>.example. Guarded rather than chained with &&: a host
            # with no key must build watermarked tiles, not fail its deploy.
            if [ -f /root/.secrets/carto.env ]; then cat /root/.secrets/carto.env >> .env; fi
            # Syntax-check the new config before anything replaces the running edge:
            # a template nginx rejects would otherwise leave a crash-looping
            # container where a working one was. Uses the droplet's real
            # certificates, which `nginx -t` insists on reading.
            docker compose build edge
            docker compose run --rm --no-deps -T edge nginx -t
            docker compose up -d --build
            # `healthy` rather than an `exit 0` inside the loop: success has to
            # fall through to the edge probe below, which an early exit would
            # make dead code.
            healthy=""
            for i in $(seq 1 12); do
              if docker compose exec -T tower-finder-service \
                python3 -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')" 2>/dev/null; then
                echo "Deploy healthy after ~$((i * 5))s"
                healthy=1
                break
              fi
              echo "Waiting for health check... attempt $i/12"
              sleep 5
            done
            if [ -z "$healthy" ]; then
              echo "Health check failed after 60s"
              docker compose logs --tail=50 || true
              exit 1
            fi
            # The edge ships dark: nothing points at 8443 yet — the fleet still
            # arrives over retina-server's nginx on 443 — so the only way to
            # verify this listener before the Cloudflare Origin Rule flips is to
            # address it directly on the droplet, under the hostname it serves.
            # --resolve rather than DNS: every name resolves to Cloudflare, not to
            # the droplet, and this probe must reach the local listener directly.
            # An unverified peer needs a Cloudflare client certificate; this
            # probe is exempt because it arrives from the droplet itself.
            edge_ok=""
            for i in $(seq 1 12); do
              code="$(curl -sk -o /dev/null -w '%{http_code}' --max-time 10 \
                --resolve "towers.retina.fm:8443:127.0.0.1" \
                "https://towers.retina.fm:8443/api/health" || true)"
              if [ "$code" = "200" ]; then
                echo "Edge answered 200 on 8443 after ~$((i * 5))s"
                edge_ok=1
                break
              fi
              echo "Waiting for the edge on 8443... attempt $i/12 (last: ${code:-no answer})"
              sleep 5
            done
            if [ -z "$edge_ok" ]; then
              echo "Edge did not answer 200 on https://towers.retina.fm:8443/api/health"
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            # The whole rendered config, not a sampled header: a header present
            # since the day it was added proves nothing about which config is
            # loaded. sed reproduces the entrypoint's envsubst exactly, because
            # TFS_EDGE_HOST is the template's only variable.
            sed 's|${TFS_EDGE_HOST}|towers.retina.fm|g' deploy/nginx/edge.conf.template > /tmp/edge-expected.conf
            if ! docker compose exec -T edge cat /etc/nginx/conf.d/default.conf > /tmp/edge-running.conf; then
              echo "Could not read the edge's running config."
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            if ! diff -u /tmp/edge-expected.conf /tmp/edge-running.conf; then
              echo "The edge is serving a config this deploy did not render."
              exit 1
            fi
            # The document, not only the API: a frontend build that leaves no
            # index.html keeps /api/health at 200 while / becomes a 404.
            edge_doc="$(curl -sk -o /dev/null -w '%{http_code}' --max-time 30 \
              --resolve "towers.retina.fm:8443:127.0.0.1" "https://towers.retina.fm:8443/" || echo unreachable)"
            if [ "$edge_doc" != "200" ]; then
              echo "The edge served / as $edge_doc."
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            # Which of three near-identical stacks answered, and the one thing
            # the public smoke cannot establish: retina-server's
            # towers.retina.fm vhost forwards only the tower paths here, so
            # /api/health on 443 is its own backend's and carries no environment.
            edge_env="$(curl -sk --max-time 10 \
              --resolve "towers.retina.fm:8443:127.0.0.1" \
              "https://towers.retina.fm:8443/api/health" \
              | sed -n 's/.*"environment"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' || true)"
            if [ "$edge_env" != "prod" ]; then
              echo "Edge on 8443 reports environment '${edge_env:-?}', expected 'prod'"
              docker compose logs --tail=50 || true
              exit 1
            fi
            echo "Edge on 8443 reports environment prod"

  smoke-prod:
    runs-on: ubuntu-latest
    needs: [deploy-prod]
    # Every check carries curl's --max-time plus a retry, so a wholly
    # unreachable production could otherwise sit here for ~11 minutes.
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - name: Smoke test production
        env:
          BASE_URL: https://towers.retina.fm
        run: bash deploy/smoke-test.sh

  deploy-test:
    runs-on: ubuntu-latest
    needs: [test, frontend]
    # Dispatch only: retina-test is for rehearsing a change, not for every merge.
    if: github.event_name == 'workflow_dispatch'
    concurrency:
      group: tf-svc-deploy-test
      cancel-in-progress: false
    env:
      DEPLOY_REF: ${{ github.sha }}
    steps:
      - name: Check required secrets
        env:
          TEST_HOST: ${{ secrets.TEST_HOST }}
          TEST_SSH_KEY: ${{ secrets.TEST_SSH_KEY }}
        run: |
          test -n "$TEST_HOST" || { echo "TEST_HOST is not set"; exit 1; }
          test -n "$TEST_SSH_KEY" || { echo "TEST_SSH_KEY is not set"; exit 1; }
      - name: Deploy to test via SSH
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1.2.5
        with:
          host: ${{ secrets.TEST_HOST }}
          username: root
          key: ${{ secrets.TEST_SSH_KEY }}
          command_timeout: 20m
          envs: APP_DIR,DEPLOY_REF
          script: |
            set -euo pipefail
            test "$(hostname)" = "retina-test" || {
              echo "Refusing to deploy test to $(hostname)"; exit 1; }
            cd "$APP_DIR"
            # Dispatching from a feature branch rehearses that branch, which is
            # the whole point of this environment.
            git fetch origin "$DEPLOY_REF"
            git reset --hard FETCH_HEAD
            test -f backend/.env || {
              echo "backend/.env is missing; secrets would be lost"; exit 1; }
            cp deploy/env.test.example .env
            # Top up with this host's own CARTO basemap key — see the note in
            # deploy/env.<env>.example. Guarded rather than chained with &&: a host
            # with no key must build watermarked tiles, not fail its deploy.
            if [ -f /root/.secrets/carto.env ]; then cat /root/.secrets/carto.env >> .env; fi
            # Syntax-check the new config before anything replaces the running edge:
            # a template nginx rejects would otherwise leave a crash-looping
            # container where a working one was. Uses the droplet's real
            # certificates, which `nginx -t` insists on reading.
            docker compose build edge
            docker compose run --rm --no-deps -T edge nginx -t
            docker compose up -d --build
            healthy=""
            for i in $(seq 1 12); do
              if docker compose exec -T tower-finder-service \
                python3 -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')" 2>/dev/null; then
                echo "Deploy healthy after ~$((i * 5))s"
                healthy=1
                break
              fi
              echo "Waiting for health check... attempt $i/12"
              sleep 5
            done
            if [ -z "$healthy" ]; then
              echo "Health check failed after 60s"
              docker compose logs --tail=50 || true
              exit 1
            fi
            # The edge ships dark: nothing points at 8443 yet — the fleet still
            # arrives over retina-server's nginx on 443 — so the only way to
            # verify this listener before the Cloudflare Origin Rule flips is to
            # address it directly on the droplet, under the hostname it serves.
            # --resolve rather than DNS: every name resolves to Cloudflare, not to
            # the droplet, and this probe must reach the local listener directly.
            # An unverified peer needs a Cloudflare client certificate; this
            # probe is exempt because it arrives from the droplet itself.
            edge_ok=""
            for i in $(seq 1 12); do
              code="$(curl -sk -o /dev/null -w '%{http_code}' --max-time 10 \
                --resolve "test-towers.retina.fm:8443:127.0.0.1" \
                "https://test-towers.retina.fm:8443/api/health" || true)"
              if [ "$code" = "200" ]; then
                echo "Edge answered 200 on 8443 after ~$((i * 5))s"
                edge_ok=1
                break
              fi
              echo "Waiting for the edge on 8443... attempt $i/12 (last: ${code:-no answer})"
              sleep 5
            done
            if [ -z "$edge_ok" ]; then
              echo "Edge did not answer 200 on https://test-towers.retina.fm:8443/api/health"
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            # The whole rendered config, not a sampled header: a header present
            # since the day it was added proves nothing about which config is
            # loaded. sed reproduces the entrypoint's envsubst exactly, because
            # TFS_EDGE_HOST is the template's only variable.
            sed 's|${TFS_EDGE_HOST}|test-towers.retina.fm|g' deploy/nginx/edge.conf.template > /tmp/edge-expected.conf
            if ! docker compose exec -T edge cat /etc/nginx/conf.d/default.conf > /tmp/edge-running.conf; then
              echo "Could not read the edge's running config."
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            if ! diff -u /tmp/edge-expected.conf /tmp/edge-running.conf; then
              echo "The edge is serving a config this deploy did not render."
              exit 1
            fi
            # The document, not only the API: a frontend build that leaves no
            # index.html keeps /api/health at 200 while / becomes a 404.
            edge_doc="$(curl -sk -o /dev/null -w '%{http_code}' --max-time 30 \
              --resolve "test-towers.retina.fm:8443:127.0.0.1" "https://test-towers.retina.fm:8443/" || echo unreachable)"
            if [ "$edge_doc" != "200" ]; then
              echo "The edge served / as $edge_doc."
              docker compose logs --tail=50 edge || true
              exit 1
            fi
            # Same droplet-local verification as deploy-staging, same reasons.
            EXPECT_ENV=test bash deploy/smoke-local.sh
