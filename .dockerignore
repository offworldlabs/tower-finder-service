.git
.venv
__pycache__
*.py[cod]
*.egg-info
.pytest_cache
.ruff_cache
data/runtime
# frontend/ used to be excluded wholesale, when it held nothing but reference
# snippets. It is the real UI now and the image builds it (see Dockerfile), so
# only the generated + installed directories are kept out.
frontend/node_modules
frontend/dist
frontend/test-results
frontend/playwright-report
docs
backend/tests
.github
# Secrets. `COPY backend/` would otherwise bake backend/.env into an image
# layer, and every token rotation would bust that layer's cache.
.env
backend/.env
